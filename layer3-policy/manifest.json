{
  "description": "S3 delta policy manifest — each release is a separate versioned file. The postStartCommand fetches only deltas with version higher than the image baseline.",
  "deltaReleases": []
}
